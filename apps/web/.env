# React version
VITE_APP_VERSION=$npm_package_version

# Directory of *.json song files the dev server reads presets from and saves
# back to. Set a real path in .env.local (git-ignored) — leave unset to fall
# back to the bundled example song.
# SONGS_DIR=
